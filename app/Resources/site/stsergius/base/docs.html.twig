{% macro docs(article) %}

		{% for document in article.documents %}
		<div>
			<a href="{{ document.path }}" target="_blank">
				<i class="{{ document.type.iconClass }}"></i>
				<span style="margin-left:5 px;">
			       	{{ document.description }}
				</span>
			</a>
		</div>
		{% endfor %} 

{% endmacro %} 